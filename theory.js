// ============ ТЕОРИЯ И ЗАДАНИЯ ДЛЯ ВСЕХ 99 УРОКОВ ============
const lessonsContent = {
    // ==================== МОДУЛЬ A1 (14 уроков) ====================
    A1_1: {
        title: "Переменные в Python",
        theory: `
            <h4><i class="fas fa-database"></i> Что такое переменная?</h4>
            <p>Переменная — это именованный контейнер для хранения данных. Представьте, что это коробка с наклейкой, на которой написано имя. Внутри коробки можно хранить числа, текст, списки и другие данные. В Python переменная создаётся автоматически в момент присваивания значения.</p>
            
            <h4><i class="fas fa-code"></i> Как создать переменную?</h4>
            <p>В Python переменная создаётся простым присваиванием значения через знак <code>=</code>. Не нужно объявлять тип заранее — Python сам определит его по значению.</p>
            <pre><code>x = 5          # создали переменную x со значением 5 (целое число)
name = "Анна"   # создали переменную name со значением "Анна" (строка)
is_student = True  # создали переменную is_student со значением True (булево значение)</code></pre>
            
            <h4><i class="fas fa-list"></i> Правила именования переменных</h4>
            <ul>
                <li>Имя должно начинаться с буквы или подчёркивания (<code>_</code>)</li>
                <li>Может содержать буквы, цифры и подчёркивания</li>
                <li>Не может содержать пробелы и спецсимволы (<code>@, #, $, %</code>)</li>
                <li>Имена чувствительны к регистру: <code>name</code> и <code>Name</code> — разные переменные</li>
                <li>Нельзя использовать ключевые слова Python (<code>if, for, while, def, class</code> и др.)</li>
            </ul>
            
            <h4><i class="fas fa-check-circle"></i> Примеры правильных имён:</h4>
            <pre><code>user_name = "Мария"
age1 = 25
_count = 10
myVariable = "Hello"</code></pre>
            
            <h4><i class="fas fa-times-circle"></i> Примеры неправильных имён:</h4>
            <pre><code>1user = "Мария"     # нельзя начинать с цифры
my-name = "Мария"   # дефис не разрешён
if = 5              # if — ключевое слово</code></pre>
        `,
        task: {
            description: "Создайте переменную с именем `name` и присвойте ей значение `'Анна'`. Затем выведите значение этой переменной на экран.",
            requirements: "• Используй оператор присваивания `=`\n• Для вывода используй функцию `print()`\n• Имя переменной должно быть `name`\n• Значение переменной должно быть строкой `'Анна'`",
            hint: "Сначала создай переменную с именем name и значением 'Анна' с помощью знака =. Затем используй функцию print(), чтобы показать содержимое переменной на экране.",
            type: "code",
            validator: (code) => code.includes('name') && code.includes('print') && (code.includes("'Анна'") || code.includes('"Анна"'))
        }
    },
    
    A1_2: {
        title: "Типы данных в Python",
        theory: `
            <h4><i class="fas fa-tag"></i> Основные типы данных</h4>
            <p>В Python есть несколько встроенных типов данных. Самые основные: <code>int</code> (целые числа), <code>float</code> (дробные числа), <code>str</code> (строки) и <code>bool</code> (булевы значения). Тип переменной определяется автоматически на основе присвоенного значения.</p>
            
            <h4><i class="fas fa-search"></i> Как узнать тип переменной?</h4>
            <p>Функция <code>type()</code> возвращает тип данных переменной. Это полезно для отладки и проверки типов.</p>
            <pre><code>x = 5
print(type(x))  # &lt;class 'int'&gt;

y = 3.14
print(type(y))  # &lt;class 'float'&gt;

name = "Анна"
print(type(name))  # &lt;class 'str'&gt;

is_ready = True
print(type(is_ready))  # &lt;class 'bool'&gt;</code></pre>
            
            <h4><i class="fas fa-info-circle"></i> Важно!</h4>
            <p>В Python тип переменной может меняться во время выполнения программы. Например, если сначала присвоить переменной число, а потом строку — это допустимо, но обычно не рекомендуется.</p>
        `,
        task: {
            description: "Создайте переменную `x` и присвойте ей целое число 5. Затем с помощью функции `type()` выведите тип этой переменной на экран.",
            requirements: "• Переменная должна называться `x`\n• Значение переменной должно быть целым числом 5\n• Для вывода типа используй `print(type(x))`",
            hint: "Сначала создай переменную x со значением 5. Затем внутри print() напиши type(x), чтобы узнать тип переменной.",
            type: "code",
            validator: (code) => code.includes('x = 5') && code.includes('print') && code.includes('type')
        }
    },
    
    A1_3: {
        title: "Функция print()",
        theory: `
            <h4><i class="fas fa-print"></i> Что такое print()?</h4>
            <p><code>print()</code> — это встроенная функция Python, которая выводит текст или значения на экран. Это основной способ показать результат работы программы пользователю. Без <code>print()</code> пользователь не увидит, что делает программа.</p>
            
            <h4><i class="fas fa-code"></i> Примеры использования:</h4>
            <pre><code>print("Привет, мир!")     # выведет: Привет, мир!
print(42)                 # выведет: 42
print(3.14)               # выведет: 3.14
print(5 + 3)              # выведет: 8</code></pre>
            
            <h4><i class="fas fa-list"></i> Вывод нескольких значений</h4>
            <p>Можно выводить несколько значений через запятую — они автоматически разделятся пробелом:</p>
            <pre><code>print("Результат:", 10, "очков")  # выведет: Результат: 10 очков</code></pre>
            
            <h4><i class="fas fa-cog"></i> Параметр end</h4>
            <p>По умолчанию <code>print()</code> переводит строку после вывода. Чтобы этого избежать, используй параметр <code>end</code>:</p>
            <pre><code>print("Привет", end=" ")
print("мир!")  # выведет: Привет мир!</code></pre>
        `,
        task: {
            description: "Напишите код, который выводит на экран фразу 'Привет, мир!'.",
            requirements: "• Используй функцию `print()`\n• Текст должен быть заключён в кавычки (одинарные или двойные)\n• Вывод должен быть именно: Привет, мир!",
            hint: "Используй функцию print(). Текст, который нужно вывести, помести в круглые скобки и обязательно заключи в кавычки.",
            type: "code",
            validator: (code) => code.includes('print') && (code.includes("'Привет, мир!'") || code.includes('"Привет, мир!"'))
        }
    },
    
    A1_4: {
        title: "Функция input()",
        theory: `
            <h4><i class="fas fa-keyboard"></i> Что такое input()?</h4>
            <p><code>input()</code> — это встроенная функция Python, которая позволяет программе получить данные от пользователя с клавиатуры. Программа приостанавливается и ждёт, пока пользователь что-то введёт и нажмёт Enter.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>name = input()
print("Привет, " + name)</code></pre>
            <p>Если пользователь введёт 'Анна', программа выведет 'Привет, Анна'.</p>
            
            <h4><i class="fas fa-exclamation-triangle"></i> Важно!</h4>
            <p>Функция <code>input()</code> всегда возвращает строку (<code>str</code>), даже если пользователь ввёл число. Если нужно получить число, используй преобразование типов:</p>
            <pre><code>age = int(input())  # преобразует введённую строку в целое число</code></pre>
            
            <h4><i class="fas fa-comment"></i> Подсказка в input()</h4>
            <p>Можно передать строку-подсказку, которая будет показана пользователю перед вводом:</p>
            <pre><code>name = input("Введите ваше имя: ")</code></pre>
        `,
        task: {
            description: "Напишите программу, которая запрашивает у пользователя его имя и выводит приветствие 'Привет, [имя]!' (где [имя] — то, что ввёл пользователь).",
            requirements: "• Используй `input()` для получения имени\n• Сохрани результат в переменную `name`\n• Используй `print()` для вывода приветствия\n• Вывод должен быть в формате: Привет, [имя]!",
            hint: "Сначала используй input() и сохрани результат в переменную name. Затем в print() используй f-строку (f перед кавычками) или сложение строк через +, чтобы вставить имя в приветствие.",
            type: "code",
            validator: (code) => code.includes('input') && code.includes('print') && code.includes('name')
        }
    },
    
    A1_5: {
        title: "Приведение типов",
        theory: `
            <h4><i class="fas fa-exchange-alt"></i> Что такое приведение типов?</h4>
            <p>Приведение типов (type casting) — это преобразование значения из одного типа данных в другой. В Python есть встроенные функции для этого: <code>int()</code>, <code>float()</code>, <code>str()</code>, <code>bool()</code>.</p>
            
            <h4><i class="fas fa-code"></i> Основные функции приведения:</h4>
            <ul>
                <li><code>int()</code> — преобразует в целое число</li>
                <li><code>float()</code> — преобразует в дробное число</li>
                <li><code>str()</code> — преобразует в строку</li>
                <li><code>bool()</code> — преобразует в булево значение</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>x = "5"           # строка
y = int(x)        # теперь y = 5 (число)
print(y + 3)      # 8

z = 10
s = str(z)        # s = "10" (строка)
print("Число: " + s)  # Число: 10

a = 7
b = float(a)      # b = 7.0 (дробное число)</code></pre>
            
            <h4><i class="fas fa-exclamation-triangle"></i> Важно!</h4>
            <p>Не все строки можно преобразовать в число. Например, <code>int("Hello")</code> вызовет ошибку. Строка должна содержать только цифры (и, возможно, знак минуса).</p>
        `,
        task: {
            description: "Дана переменная `x = \"5\"` (строка). Преобразуйте её в целое число, сохраните результат в переменную `y` и выведите результат сложения `y + 3`.",
            requirements: "• Используй `int()` для преобразования\n• Переменная `y` должна содержать преобразованное значение\n• Выведи `y + 3` с помощью `print()`",
            hint: "Функция int() преобразует строку в число. Примени её к x и сохрани результат в y. Затем внутри print() напиши y + 3.",
            type: "code",
            validator: (code) => code.includes('int(x)') && code.includes('print') && code.includes('y')
        }
    },
    
    A1_6: {
        title: "Арифметические операции",
        theory: `
            <h4><i class="fas fa-calculator"></i> Арифметические операторы в Python</h4>
            <p>Python поддерживает все основные математические операции. Вот полный список операторов:</p>
            <ul>
                <li><code>+</code> — сложение</li>
                <li><code>-</code> — вычитание</li>
                <li><code>*</code> — умножение</li>
                <li><code>/</code> — деление (всегда возвращает float)</li>
                <li><code>//</code> — целочисленное деление (возвращает целую часть)</li>
                <li><code>%</code> — остаток от деления</li>
                <li><code>**</code> — возведение в степень</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>print(5 + 3)    # 8
print(10 - 4)   # 6
print(7 * 6)    # 42
print(10 / 3)   # 3.3333333333333335
print(10 // 3)  # 3 (целая часть)
print(10 % 3)   # 1 (остаток)
print(2 ** 4)   # 16 (2 в 4 степени)</code></pre>
            
            <h4><i class="fas fa-sort-amount-up"></i> Порядок операций</h4>
            <p>Python соблюдает стандартный математический порядок: сначала скобки, затем степень, потом умножение/деление, затем сложение/вычитание.</p>
        `,
        task: {
            description: "Вычислите 5 в 3 степени (5³). Результат выведите на экран с помощью `print()`.",
            requirements: "• Используй оператор `**` для возведения в степень\n• Выражение должно быть: 5 ** 3\n• Результат выведи через `print()`",
            hint: "Для возведения в степень используется оператор **. Напиши 5 ** 3 внутри print().",
            type: "code",
            validator: (code) => code.includes('5 ** 3') && code.includes('print')
        }
    },
    
    A1_7: {
        title: "Строки и индексы",
        theory: `
            <h4><i class="fas fa-font"></i> Строки в Python</h4>
            <p>Строка — это последовательность символов. Каждый символ имеет свой индекс — порядковый номер. Индексация в Python начинается с <strong>0</strong>. Это значит, что первый символ строки имеет индекс 0, второй — 1, и так далее.</p>
            
            <h4><i class="fas fa-sort-numeric-up"></i> Индексация строк:</h4>
            <p>Рассмотрим строку <code>"Анна"</code>:</p>
            <ul>
                <li>Индекс 0: 'А'</li>
                <li>Индекс 1: 'н'</li>
                <li>Индекс 2: 'н'</li>
                <li>Индекс 3: 'а'</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Доступ к символам по индексу:</h4>
            <pre><code>name = "Анна"
print(name[0])  # выведет: А
print(name[1])  # выведет: н
print(name[2])  # выведет: н
print(name[3])  # выведет: а</code></pre>
            
            <h4><i class="fas fa-arrow-left"></i> Отрицательные индексы</h4>
            <p>Можно использовать отрицательные индексы для доступа с конца строки:</p>
            <pre><code>print(name[-1])  # выведет: а (последний символ)
print(name[-2])  # выведет: н (предпоследний)</code></pre>
            
            <h4><i class="fas fa-ruler"></i> Длина строки</h4>
            <p>Функция <code>len()</code> возвращает длину строки (количество символов).</p>
        `,
        task: {
            description: "Дана строка `name = \"Анна\"`. Выведите на экран второй символ этой строки (букву 'н').",
            requirements: "• Используй квадратные скобки `[]` для доступа к символу\n• Второй символ имеет индекс 1 (так как счёт начинается с 0)\n• Используй `print()` для вывода",
            hint: "Чтобы получить символ строки, используй квадратные скобки после имени переменной и укажи внутри индекс нужного символа. Помни, что первый символ — индекс 0, значит второй — индекс 1.",
            type: "code",
            validator: (code) => code.includes('print') && code.includes('name[1]')
        }
    },
    
    A1_8: {
        title: "f-строки (форматирование)",
        theory: `
            <h4><i class="fas fa-magic"></i> Что такое f-строки?</h4>
            <p>f-строки (formatted strings) — это удобный способ вставлять значения переменных прямо внутрь строки. Они появились в Python 3.6 и стали стандартом форматирования. Это самый современный и читаемый способ работы со строками.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <p>Перед открывающей кавычкой ставится буква <code>f</code>, а внутри строки переменные указываются в фигурных скобках <code>{}</code>:</p>
            <pre><code>name = "Анна"
print(f"Привет, {name}")  # выведет: Привет, Анна</code></pre>
            
            <h4><i class="fas fa-code"></i> Другие примеры:</h4>
            <pre><code>age = 25
print(f"Мне {age} лет")  # Мне 25 лет

x = 10
y = 20
print(f"{x} + {y} = {x + y}")  # 10 + 20 = 30</code></pre>
            
            <h4><i class="fas fa-star"></i> Преимущества f-строк:</h4>
            <ul>
                <li>Код становится чище и понятнее</li>
                <li>Можно выполнять выражения внутри <code>{}</code></li>
                <li>Работает быстрее, чем старые методы форматирования</li>
            </ul>
            
            <h4><i class="fas fa-link"></i> Старый способ (конкатенация):</h4>
            <pre><code>print("Привет, " + name + "!")  # тоже работает, но менее удобно</code></pre>
        `,
        task: {
            description: "Создайте переменную `name` со значением `'Анна'`. Выведите на экран приветствие в формате 'Привет, Анна' с использованием f-строки.",
            requirements: "• Создай переменную `name = 'Анна'`\n• Используй f-строку: перед кавычками поставь `f`\n• Внутри строки используй `{name}` для вставки значения\n• Используй `print()` для вывода",
            hint: "Сначала создай переменную name = 'Анна'. Затем в print() напиши f перед открывающей кавычкой, а внутри строки в фигурных скобках укажи имя переменной — {name}.",
            type: "code",
            validator: (code) => code.includes('name =') && (code.includes('f"') || code.includes("f'")) && code.includes('print')
        }
    },
    
    A1_9: {
        title: "Булевы значения и сравнения",
        theory: `
            <h4><i class="fas fa-check-circle"></i> Булевы значения</h4>
            <p>В Python есть специальный тип данных <code>bool</code> (булевый), который может принимать только два значения: <code>True</code> (истина) и <code>False</code> (ложь). Эти значения используются в условных операторах и циклах.</p>
            
            <h4><i class="fas fa-code-branch"></i> Операторы сравнения</h4>
            <p>Эти операторы возвращают булевы значения:</p>
            <ul>
                <li><code>==</code> — равно</li>
                <li><code>!=</code> — не равно</li>
                <li><code>></code> — больше</li>
                <li><code><</code> — меньше</li>
                <li><code>>=</code> — больше или равно</li>
                <li><code><=</code> — меньше или равно</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>print(5 == 5)   # True (5 равно 5)
print(5 == 3)   # False (5 не равно 3)
print(5 > 3)    # True (5 больше 3)
print(5 < 3)    # False (5 не меньше 3)</code></pre>
            
            <h4><i class="fas fa-exclamation-triangle"></i> Важно!</h4>
            <p>Оператор <code>=</code> (один знак) — это присваивание, а <code>==</code> (два знака) — это сравнение. Не путай их! Использование <code>=</code> вместо <code>==</code> в условиях приведёт к ошибке или неожиданному поведению.</p>
        `,
        task: {
            description: "Какое значение вернёт выражение 5 == 5? Напишите код, который выводит результат этого сравнения.",
            requirements: "• Используй оператор сравнения `==`\n• Выражение должно быть: 5 == 5\n• Используй `print()` для вывода результата",
            hint: "Оператор == проверяет равенство. Напиши 5 == 5 внутри print(). Результатом будет True или False.",
            type: "code",
            validator: (code) => code.includes('5 == 5') && code.includes('print')
        }
    },
    
    A1_10: {
        title: "Условный оператор if",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Что такое if?</h4>
            <p><code>if</code> — это условный оператор, который позволяет выполнять разные блоки кода в зависимости от условия. Если условие истинно (True), код внутри if выполняется; если ложно (False) — пропускается. Это основа для создания логики в программах.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>if условие:
    # код, который выполнится, если условие True</code></pre>
            <p>Важно! После условия ставится двоеточие <code>:</code>, а сам код внутри if пишется с отступом (обычно 4 пробела).</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>age = 18
if age >= 18:
    print("Доступ разрешён")</code></pre>
            <p>Если age больше или равно 18, выведется 'Доступ разрешён'. Если меньше — ничего не выведется.</p>
            
            <h4><i class="fas fa-code"></i> Другие примеры:</h4>
            <pre><code>x = 10
if x > 5:
    print("x больше 5")

if x == 10:
    print("x равен 10")</code></pre>
        `,
        task: {
            description: "Напишите код, который проверяет, что переменная `x` больше 5. Если это так, выведите на экран сообщение 'Больше'.",
            requirements: "• Создай переменную `x = 10`\n• Используй условный оператор `if`\n• Условие: `x > 5`\n• При истинности условия выведи `'Больше'` через `print()`",
            hint: "Сначала создай переменную x = 10. Затем напиши if, после него условие x > 5 и двоеточие. На следующей строке с отступом напиши print('Больше').",
            type: "code",
            validator: (code) => code.includes('if') && code.includes('x > 5') && code.includes('print')
        }
    },
    
    A1_11: {
        title: "Практика: ввод и вывод",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>В этом уроке мы объединим знания о вводе данных (<code>input()</code>) и выводе (<code>print()</code>). Это одни из самых важных функций для взаимодействия с пользователем. Программа, которая не может получить данные от пользователя и показать результат, бесполезна.</p>
            
            <h4><i class="fas fa-lightbulb"></i> Напоминание:</h4>
            <ul>
                <li><code>input()</code> — читает строку с клавиатуры</li>
                <li><code>print()</code> — выводит текст на экран</li>
                <li>Можно комбинировать строки через <code>+</code> или использовать f-строки</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Пример того, что нужно сделать:</h4>
            <pre><code>name = input()
print("Привет, " + name + "!")</code></pre>
            <p>Если пользователь введёт 'Анна', программа выведет 'Привет, Анна!'.</p>
        `,
        task: {
            description: "Напишите программу, которая запрашивает у пользователя его имя, а затем выводит сообщение 'Привет, [имя]!'. Используйте input() и print().",
            requirements: "• Используй `input()` для получения имени\n• Сохрани результат в переменную (например, `name`)\n• Используй `print()` для вывода приветствия\n• Формат вывода: 'Привет, [имя]!'",
            hint: "Сначала используй input() и сохрани результат в переменную (например, name). Затем в print() используй f-строку (f перед кавычками) или сложи строки через +, чтобы вставить имя в приветствие.",
            type: "code",
            validator: (code) => code.includes('input') && code.includes('print')
        }
    },
    
    A1_12: {
        title: "Практика: арифметика",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>В этом уроке потренируемся вычислять площадь прямоугольника. Площадь — это одна из самых простых и полезных математических операций, которая часто встречается в программировании.</p>
            
            <h4><i class="fas fa-draw-polygon"></i> Формула площади прямоугольника:</h4>
            <p><code>Площадь = длина × ширина</code></p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <p>Если длина = 5, ширина = 3, то площадь = 5 × 3 = 15</p>
            
            <h4><i class="fas fa-bullseye"></i> Что нужно сделать:</h4>
            <p>Вам даны переменные <code>a = 5</code> и <code>b = 3</code>. Вычислите их произведение и выведите результат.</p>
        `,
        task: {
            description: "Даны переменные a = 5 и b = 3. Вычислите их произведение (площадь прямоугольника) и выведите результат на экран.",
            requirements: "• Используй оператор умножения `*`\n• Результат сохрани в переменную `s`\n• Выведи `s` с помощью `print()`",
            hint: "Умножение в Python обозначается звёздочкой *. Вычисли a * b и сохрани результат в новую переменную s. Затем выведи s через print().",
            type: "code",
            validator: (code) => code.includes('*') && code.includes('print')
        }
    },
    
    A1_13: {
        title: "Практика: обмен значений",
        theory: `
            <h4><i class="fas fa-exchange-alt"></i> Обмен значениями переменных</h4>
            <p>Иногда нужно поменять значения двух переменных местами. Например, при сортировке или обработке данных. В Python это можно сделать очень элегантно и просто.</p>
            
            <h4><i class="fas fa-code"></i> Способ 1 (классический, с временной переменной):</h4>
            <pre><code>a = 5
b = 10
temp = a
a = b
b = temp</code></pre>
            <p>Здесь мы используем дополнительную переменную <code>temp</code>, чтобы временно сохранить значение <code>a</code>.</p>
            
            <h4><i class="fas fa-star"></i> Способ 2 (Python way):</h4>
            <pre><code>a = 5
b = 10
a, b = b, a  # теперь a = 10, b = 5</code></pre>
            <p>Это более короткий и читаемый способ. Python создаёт кортеж (b, a), а затем распаковывает его в переменные a и b.</p>
            
            <h4><i class="fas fa-lightbulb"></i> Почему это работает?</h4>
            <p>В Python справа сначала создаётся кортеж (b, a), а затем он распаковывается в переменные a и b. Это очень удобно и читаемо.</p>
        `,
        task: {
            description: "Даны переменные a = 5 и b = 10. Поменяйте их значения местами так, чтобы a стало равно 10, а b стало равно 5. Выведите значения a и b.",
            requirements: "• Используй конструкцию `a, b = b, a`\n• После обмена выведи a и b через `print()`",
            hint: "В Python есть короткий способ обмена значений: a, b = b, a. Это поменяет значения местами. После этого выведи a и b через print().",
            type: "code",
            validator: (code) => (code.includes('a, b = b, a') || (code.includes('temp') && code.includes('a') && code.includes('b'))) && code.includes('print')
        }
    },
    
    A1_14: {
        title: "Повторение модуля A1",
        theory: `
            <h4><i class="fas fa-review"></i> Повторение: что мы изучили в модуле A1</h4>
            <p>Поздравляю! Вы прошли первый модуль. Давайте вспомним все темы:</p>
            <ul>
                <li>Переменные и правила их именования</li>
                <li>Типы данных: int, float, str, bool</li>
                <li>Функции print() и input()</li>
                <li>Приведение типов: int(), str(), float()</li>
                <li>Арифметические операции: +, -, *, /, //, %, **</li>
                <li>Строки и доступ к символам по индексу</li>
                <li>f-строки для форматирования</li>
                <li>Булевы значения и операторы сравнения</li>
                <li>Условный оператор if</li>
            </ul>
            
            <h4><i class="fas fa-question-circle"></i> Задание на проверку знаний</h4>
            <p>Ответьте на вопрос о типах данных в Python.</p>
        `,
        task: {
            description: "Какие из перечисленных типов данных существуют в Python? Отметьте все правильные варианты.",
            requirements: "• Выбери все правильные варианты\n• В Python есть: int (целые числа), str (строки), float (дробные числа)\n• char (символ) в Python нет — символы хранятся как строки длиной 1",
            hint: "В Python есть int для целых чисел, float для дробных и str для строк. Отдельного типа для одного символа (char) в Python нет — символы хранятся как строки длиной 1.",
            type: "checkbox",
            options: ["int", "str", "float", "char"],
            correct: [0, 1, 2]
        }
    },

    // ==================== МОДУЛЬ A2 (Условия) ====================
    A2_1: {
        title: "Условный оператор if",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Что такое if?</h4>
            <p><code>if</code> — это условный оператор, который позволяет выполнять разные блоки кода в зависимости от условия. Если условие истинно (True), код внутри if выполняется; если ложно (False) — пропускается.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>if условие:
    # код, который выполнится, если условие True</code></pre>
            <p>Важно! После условия ставится двоеточие <code>:</code>, а сам код внутри if пишется с отступом (обычно 4 пробела).</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>x = 10
if x > 5:
    print("Больше")</code></pre>
            <p>Если x больше 5, выведется 'Больше'. Если x меньше или равно 5 — ничего не выведется.</p>
        `,
        task: {
            description: "Напишите код, который проверяет, что переменная x > 5. Если условие истинно, выведите 'Больше'.",
            requirements: "• Создай x = 10\n• Используй if\n• Условие: x > 5\n• Вывод: 'Больше'",
            hint: "Создай переменную x = 10. Затем напиши if, после него условие x > 5 и двоеточие. На следующей строке с отступом напиши print('Больше').",
            type: "code",
            validator: (code) => code.includes('if') && code.includes('x > 5')
        }
    },
    
    A2_2: {
        title: "Оператор else",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Что такое else?</h4>
            <p><code>else</code> — это часть условного оператора, которая выполняется, если условие в if ложно (False). Это позволяет обработать оба варианта: и когда условие истинно, и когда ложно.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>if условие:
    # выполняется, если условие True
else:
    # выполняется, если условие False</code></pre>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>x = 3
if x > 5:
    print("Больше")
else:
    print("Меньше или равно")</code></pre>
            <p>Так как x = 3, условие x > 5 ложно, поэтому выполнится блок else и выведется 'Меньше или равно'.</p>
        `,
        task: {
            description: "Дан код с пропуском. Вставьте правильное ключевое слово, чтобы код работал корректно.",
            requirements: "• Вставь 'else' вместо пропуска",
            type: "fill",
            code: "x = 3\nif x > 5:\n    print('A')\n___:\n    print('B')",
            correct: "else"
        }
    },
    
    A2_3: {
        title: "Оператор elif",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Что такое elif?</h4>
            <p><code>elif</code> — это сокращение от "else if". Он позволяет проверить несколько условий подряд. Если первое условие ложно, проверяется второе, и так далее. Как только находится истинное условие, выполняется соответствующий блок кода, и вся конструкция завершается.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>if условие1:
    # выполняется, если условие1 True
elif условие2:
    # выполняется, если условие1 False и условие2 True
elif условие3:
    # выполняется, если условие1 и условие2 False, а условие3 True
else:
    # выполняется, если все условия ложны</code></pre>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>mark = 75
if mark >= 90:
    print("5")
elif mark >= 70:
    print("4")
elif mark >= 50:
    print("3")
else:
    print("2")</code></pre>
            <p>При mark = 75, первое условие (>=90) ложно, второе (>=70) истинно, поэтому выведется '4'.</p>
        `,
        task: {
            description: "Что выведет код при x = 75? Выберите правильный вариант.",
            requirements: "• Внимательно посмотри на условия\n• 75 >= 90? Нет\n• 75 >= 70? Да → вывод '4'",
            hint: "Проверь каждое условие по порядку: сначала 75 >= 90? Нет. Потом 75 >= 70? Да → выведется то, что в этом блоке.",
            type: "explain",
            question: "Что выведет код при x = 75?",
            options: ["5", "4", "3"],
            correct: 1
        }
    },
    
    A2_4: {
        title: "Логические операторы and, or, not",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Логические операторы</h4>
            <p>Логические операторы позволяют комбинировать несколько условий в одно. Они широко используются в условных операторах и циклах.</p>
            
            <h4><i class="fas fa-code"></i> Операторы:</h4>
            <ul>
                <li><code>and</code> — возвращает True, если оба условия истинны</li>
                <li><code>or</code> — возвращает True, если хотя бы одно условие истинно</li>
                <li><code>not</code> — возвращает противоположное значение (True → False, False → True)</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>age = 25
has_license = True

if age >= 18 and has_license:
    print("Можно водить")  # True и True → True

if age >= 18 or has_license:
    print("Хотя бы одно условие истинно")  # True

if not has_license:
    print("Нет прав")  # not True → False, код не выполнится</code></pre>
        `,
        task: {
            description: "Какие логические операторы есть в Python? Отметьте все правильные варианты.",
            requirements: "• В Python есть: and, or, not\n• xor нет",
            hint: "Логические операторы в Python — это and (И), or (ИЛИ) и not (НЕ). Оператора xor в Python нет как отдельного ключевого слова.",
            type: "checkbox",
            options: ["and", "or", "not", "xor"],
            correct: [0, 1, 2]
        }
    },
    
    A2_5: {
        title: "Вложенные условия",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Вложенные условия</h4>
            <p>Вложенные условия — это когда один условный оператор находится внутри другого. Это позволяет создавать более сложную логику, где решение зависит от нескольких факторов.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>age = 20
has_ticket = True

if age >= 18:
    if has_ticket:
        print("Проходите")
    else:
        print("Купите билет")
else:
    print("Вход только с 18 лет")</code></pre>
            <p>Сначала проверяется возраст. Если возраст подходит, затем проверяется наличие билета.</p>
            
            <h4><i class="fas fa-lightbulb"></i> Альтернатива</h4>
            <p>Вложенные условия можно заменить логическими операторами:</p>
            <pre><code>if age >= 18 and has_ticket:
    print("Проходите")
elif age >= 18:
    print("Купите билет")
else:
    print("Вход только с 18 лет")</code></pre>
        `,
        task: {
            description: "Напишите код, который проверяет, что x > 0 и x < 10. Используйте вложенный if (один if внутри другого).",
            requirements: "• Создай x = 5\n• Сначала проверь x > 0\n• Внутри проверь x < 10\n• Выведи 'Подходит' если оба условия истинны",
            hint: "Сначала создай x = 5. Затем напиши if x > 0:, внутри него — ещё один if x < 10:, а внутри второго — print('Подходит'). Не забудь про отступы.",
            type: "code",
            validator: (code) => code.includes('if') && code.includes('x > 0') && code.includes('x < 10') && code.includes('print')
        }
    },
    
    A2_6: {
        title: "Тернарный оператор",
        theory: `
            <h4><i class="fas fa-code"></i> Тернарный оператор</h4>
            <p>Тернарный оператор — это короткая форма записи if-else в одну строку. Он полезен для простых условных присваиваний, когда нужно выбрать одно из двух значений.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>значение_если_True if условие else значение_если_False</code></pre>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>x = 10
result = "Больше" if x > 5 else "Меньше или равно"
print(result)  # выведет: Больше</code></pre>
            <p>Это эквивалентно:</p>
            <pre><code>if x > 5:
    result = "Больше"
else:
    result = "Меньше или равно"</code></pre>
        `,
        task: {
            description: "Вставьте пропущенное слово в тернарный оператор, чтобы код работал правильно.",
            requirements: "• Вставь 'else'",
            type: "fill",
            code: "x = 10\nresult = 'A' if x > 5 ___ 'B'",
            correct: "else"
        }
    },
    
    A2_7: {
        title: "Сравнение строк",
        theory: `
            <h4><i class="fas fa-font"></i> Сравнение строк</h4>
            <p>Строки в Python можно сравнивать с помощью операторов сравнения (<, >, <=, >=, ==, !=). Сравнение происходит посимвольно по кодам Unicode. Это означает, что 'a' меньше 'b', 'A' меньше 'a' (потому что у заглавных букв коды меньше).</p>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>print("abc" == "abc")  # True
print("abc" == "abd")  # False
print("abc" < "abd")   # True (c < d)
print("Apple" < "apple")  # True (A < a)</code></pre>
            
            <h4><i class="fas fa-lightbulb"></i> Важно!</h4>
            <p>Сравнение строк регистрозависимо. 'A' и 'a' — разные символы. Для регистронезависимого сравнения используй методы <code>.lower()</code> или <code>.upper()</code>.</p>
        `,
        task: {
            description: "Напишите код, который сравнивает две строки 'abc' и 'abd' и выводит результат сравнения на экран.",
            requirements: "• Используй оператор <\n• print('abc' < 'abd')",
            hint: "Строки сравниваются посимвольно. Используй оператор < между двумя строками внутри print().",
            type: "code",
            validator: (code) => code.includes('print') && code.includes('"abc"') && code.includes('<')
        }
    },
    
    A2_8: {
        title: "Сравнение чисел",
        theory: `
            <h4><i class="fas fa-calculator"></i> Сравнение чисел</h4>
            <p>Числа в Python сравниваются стандартным математическим способом. Все операторы сравнения работают с числами: <code>></code>, <code><</code>, <code>>=</code>, <code><=</code>, <code>==</code>, <code>!=</code>.</p>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>print(5 > 3)    # True
print(5 < 3)    # False
print(5 >= 5)   # True
print(5 != 3)   # True
print(5 == 5)   # True</code></pre>
        `,
        task: {
            description: "Вставьте правильный оператор сравнения, чтобы условие проверяло, что x равно 5.",
            requirements: "• Используй оператор '=='",
            type: "fill",
            code: "if x ___ 5:\n    print('x равно 5')",
            correct: "=="
        }
    },
    
    A2_9: {
        title: "Логические цепочки",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Логические цепочки</h4>
            <p>В Python можно записывать цепочки сравнений естественным образом, как в математике. Например, <code>x > 5 and x < 10</code> можно записать как <code>5 < x < 10</code>.</p>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>x = 7
print(5 < x < 10)   # True (5 < 7 < 10)
print(5 < x < 6)    # False (7 не меньше 6)

# Эквивалентно:
print(x > 5 and x < 10)</code></pre>
            
            <h4><i class="fas fa-lightbulb"></i> Преимущества</h4>
            <p>Такой способ записи более читаемый и короткий. Он работает с любыми операторами сравнения.</p>
        `,
        task: {
            description: "Напишите код, который проверяет, что x находится между 5 и 10 (не включая границы). Используйте цепочку сравнений.",
            requirements: "• Создай x = 7\n• Используй 5 < x < 10",
            hint: "В Python можно записать двойное неравенство: 5 < x < 10. Используй это в условии if и выведи сообщение.",
            type: "code",
            validator: (code) => code.includes('5 < x < 10') && code.includes('print')
        }
    },
    
    A2_10: {
        title: "Практика: if-elif-else",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Напишем программу для оценки результатов теста. Оценка выставляется по следующей шкале:</p>
            <ul>
                <li>90 и выше — отлично (A)</li>
                <li>70-89 — хорошо (B)</li>
                <li>50-69 — удовлетворительно (C)</li>
                <li>Ниже 50 — неудовлетворительно (D)</li>
            </ul>
        `,
        task: {
            description: "Напишите программу, которая по переменной mark (например, 75) выводит соответствующую оценку (A, B, C или D).",
            requirements: "• Используй if-elif-else\n• mark = 75\n• 90+ → A, 70-89 → B, 50-69 → C, иначе D",
            hint: "Используй конструкцию if-elif-else. Сначала проверь mark >= 90 (A), затем mark >= 70 (B), затем mark >= 50 (C), иначе — D.",
            type: "code",
            validator: (code) => code.includes('if') && code.includes('elif') && code.includes('else')
        }
    },
    
    A2_11: {
        title: "Практика: логические операторы",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Логические операторы <code>and</code>, <code>or</code>, <code>not</code> позволяют комбинировать условия. Например, чтобы проверить, что человек совершеннолетний и имеет права, нужно использовать <code>and</code>.</p>
        `,
        task: {
            description: "Напишите код, который проверяет, что age >= 18 и has_license == True. Если оба условия истинны, выведите 'Можно водить'.",
            requirements: "• age = 25, has_license = True\n• Используй and",
            hint: "В условии if соедини два выражения через and: age >= 18 и has_license. Оба должны быть истинны, чтобы выполнился код внутри if.",
            type: "code",
            validator: (code) => code.includes('and') && code.includes('print')
        }
    },
    
    A2_12: {
        title: "Практика: вложенные условия",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Вложенные условия позволяют создавать более сложную логику. Например, можно сначала проверить одно условие, а внутри него — другое.</p>
        `,
        task: {
            description: "Напишите код с вложенным if: сначала проверьте, что x > 0, а внутри проверьте, что x < 10. Если оба условия истинны, выведите 'Подходит'.",
            requirements: "• x = 5\n• Вложенный if",
            hint: "Сначала напиши if x > 0:. Внутри него — ещё один if x < 10:. Внутри второго — print('Подходит'). Не забудь про отступы.",
            type: "code",
            validator: (code) => code.includes('if x > 0') && code.includes('if x < 10')
        }
    },
    
    A2_13: {
        title: "Практика: тернарный оператор",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Тернарный оператор позволяет записать простое if-else в одну строку. Формат: <code>значение_если_True if условие else значение_если_False</code>.</p>
        `,
        task: {
            description: "Вставьте пропущенное слово в тернарный оператор.",
            requirements: "• Вставь 'else'",
            type: "fill",
            code: "x = 5\nresult = 'Positive' if x > 0 ___ 'Negative'",
            correct: "else"
        }
    },
    
    A2_14: {
        title: "Повторение модуля A2",
        theory: `
            <h4><i class="fas fa-review"></i> Повторение: что мы изучили в модуле A2</h4>
            <ul>
                <li>Условный оператор if</li>
                <li>Операторы else и elif</li>
                <li>Логические операторы and, or, not</li>
                <li>Вложенные условия</li>
                <li>Тернарный оператор</li>
                <li>Сравнение строк и чисел</li>
                <li>Логические цепочки</li>
            </ul>
        `,
        task: {
            description: "Что делает elif?",
            requirements: "• Выбери правильный вариант",
            type: "checkbox",
            options: ["Проверяет дополнительное условие", "Завершает программу", "Повторяет код"],
            correct: [0]
        }
    },

    // ==================== МОДУЛЬ B1 (Циклы) ====================
    B1_1: {
        title: "Цикл for",
        theory: `
            <h4><i class="fas fa-redo-alt"></i> Что такое цикл for?</h4>
            <p>Цикл <code>for</code> используется для повторения блока кода определённое количество раз. Он итерируется по последовательности (списку, строке, диапазону чисел).</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>for переменная in последовательность:
    # код, который повторяется</code></pre>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>for i in range(5):
    print(i)  # выведет 0, 1, 2, 3, 4</code></pre>
        `,
        task: {
            description: "Напишите цикл for, который выводит числа от 0 до 4.",
            requirements: "• Используй range(5)",
            hint: "Используй конструкцию for i in range(5):, а внутри цикла — print(i). range(5) создаст числа от 0 до 4.",
            type: "code",
            validator: (code) => code.includes('for') && code.includes('range')
        }
    },
    
    B1_2: {
        title: "Функция range()",
        theory: `
            <h4><i class="fas fa-chart-line"></i> Что такое range()?</h4>
            <p>Функция <code>range()</code> генерирует последовательность чисел. Она часто используется в циклах <code>for</code>.</p>
            
            <h4><i class="fas fa-code"></i> Формы range():</h4>
            <ul>
                <li><code>range(stop)</code> — от 0 до stop-1</li>
                <li><code>range(start, stop)</code> — от start до stop-1</li>
                <li><code>range(start, stop, step)</code> — от start до stop-1 с шагом step</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>range(5)      # 0,1,2,3,4
range(2, 5)   # 2,3,4
range(1, 10, 2)  # 1,3,5,7,9</code></pre>
        `,
        task: {
            description: "Вставьте пропущенное число, чтобы цикл вывел числа от 0 до 4.",
            requirements: "• range(5)",
            type: "fill",
            code: "for i in range(___):\n    print(i)",
            correct: "5"
        }
    },
    
    B1_3: {
        title: "Цикл while",
        theory: `
            <h4><i class="fas fa-redo-alt"></i> Что такое цикл while?</h4>
            <p>Цикл <code>while</code> выполняет блок кода, пока условие истинно (True). Он используется, когда количество повторений заранее неизвестно.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>while условие:
    # код, который повторяется</code></pre>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>x = 0
while x < 3:
    print(x)
    x = x + 1  # выведет 0, 1, 2</code></pre>
        `,
        task: {
            description: "Напишите цикл while, который выводит числа 0, 1, 2.",
            requirements: "• x = 0\n• while x < 3\n• x = x + 1",
            hint: "Создай переменную x = 0. Затем напиши while x < 3:, внутри — print(x) и x = x + 1 (чтобы увеличивать x на 1).",
            type: "code",
            validator: (code) => code.includes('while') && code.includes('x < 3')
        }
    },
    
    B1_4: {
        title: "Оператор break",
        theory: `
            <h4><i class="fas fa-stop"></i> Что такое break?</h4>
            <p>Оператор <code>break</code> немедленно прерывает выполнение цикла, независимо от условия. Программа продолжает выполнение с кода после цикла.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>for i in range(10):
    if i == 3:
        break
    print(i)  # выведет 0, 1, 2</code></pre>
        `,
        task: {
            description: "Напишите цикл for, который выводит числа от 0 до 2, а при i == 3 прерывается с помощью break.",
            requirements: "• for i in range(10)\n• if i == 3: break",
            hint: "Используй цикл for i in range(10). Внутри проверь if i == 3: break. До break выводи i через print().",
            type: "code",
            validator: (code) => code.includes('for') && code.includes('break')
        }
    },
    
    B1_5: {
        title: "Оператор continue",
        theory: `
            <h4><i class="fas fa-forward"></i> Что такое continue?</h4>
            <p>Оператор <code>continue</code> пропускает текущую итерацию цикла и переходит к следующей. Код после continue в текущей итерации не выполняется.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>for i in range(5):
    if i == 2:
        continue
    print(i)  # выведет 0, 1, 3, 4 (2 пропущена)</code></pre>
        `,
        task: {
            description: "Напишите цикл for, который выводит числа от 0 до 4, пропуская число 2 с помощью continue.",
            requirements: "• for i in range(5)\n• if i == 2: continue",
            hint: "Используй цикл for i in range(5). Внутри проверь if i == 2: continue, затем выводи i через print().",
            type: "code",
            validator: (code) => code.includes('for') && code.includes('continue')
        }
    },
    
    B1_6: {
        title: "else в циклах",
        theory: `
            <h4><i class="fas fa-code"></i> else в циклах</h4>
            <p>В Python циклы <code>for</code> и <code>while</code> могут иметь блок <code>else</code>, который выполняется, если цикл завершился нормально (без <code>break</code>).</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>for i in range(3):
    print(i)
else:
    print("Цикл завершён")  # выполнится</code></pre>
        `,
        task: {
            description: "Напишите цикл for с блоком else, который выводит сообщение после завершения цикла.",
            requirements: "• for i in range(3)\n• else: print('Готово')",
            hint: "Напиши цикл for i in range(3):, внутри — print(i). После цикла добавь блок else: с print('Готово').",
            type: "code",
            validator: (code) => code.includes('for') && code.includes('else')
        }
    },
    
    B1_7: {
        title: "Вложенные циклы",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Вложенные циклы</h4>
            <p>Вложенные циклы — это цикл внутри другого цикла. Внутренний цикл выполняется полностью на каждой итерации внешнего цикла.</p>
            
            <h4><i class="fas fa-code"></i> Пример (таблица умножения):</h4>
            <pre><code>for i in range(1, 4):
    for j in range(1, 4):
        print(f"{i} * {j} = {i * j}")</code></pre>
        `,
        task: {
            description: "Напишите вложенный цикл для вывода таблицы умножения от 1 до 3.",
            requirements: "• for i in range(1, 4)\n• for j in range(1, 4)",
            hint: "Используй внешний цикл for i in range(1, 4): и внутри него — внутренний for j in range(1, 4):. Внутри выводи i * j.",
            type: "code",
            validator: (code) => code.includes('for') && code.includes('for')
        }
    },
    
    B1_8: {
        title: "Бесконечные циклы",
        theory: `
            <h4><i class="fas fa-infinity"></i> Бесконечные циклы</h4>
            <p>Бесконечный цикл — это цикл, условие которого всегда истинно. Обычно используется с <code>break</code> для выхода при определённом условии.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>while True:
    ответ = input("Введите 'выход' для остановки: ")
    if ответ == 'выход':
        break</code></pre>
        `,
        task: {
            description: "Вставьте пропущенное условие для создания бесконечного цикла.",
            requirements: "• while True:",
            type: "fill",
            code: "while ___:\n    print('бесконечно')",
            correct: "True"
        }
    },
    
    B1_9: {
        title: "Функция enumerate()",
        theory: `
            <h4><i class="fas fa-list-ol"></i> Что такое enumerate()?</h4>
            <p>Функция <code>enumerate()</code> возвращает индекс и значение элемента при итерации по последовательности. Это удобно, когда нужен не только элемент, но и его позиция.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>fruits = ['яблоко', 'банан', 'апельсин']
for i, fruit in enumerate(fruits):
    print(i, fruit)
# 0 яблоко
# 1 банан
# 2 апельсин</code></pre>
        `,
        task: {
            description: "Используйте enumerate для вывода индекса и значения из списка ['a', 'b', 'c'].",
            requirements: "• for i, val in enumerate(['a','b','c'])",
            hint: "Используй цикл for i, val in enumerate(['a','b','c']): и внутри print(i, val). enumerate вернёт индекс и элемент.",
            type: "code",
            validator: (code) => code.includes('enumerate')
        }
    },
    
    B1_10: {
        title: "Практика: for и range",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Посчитаем сумму всех чисел от 1 до 100. Это классическая задача на циклы.</p>
        `,
        task: {
            description: "Напишите цикл for, который суммирует числа от 1 до 100 и выводит результат.",
            requirements: "• total = 0\n• for i in range(1, 101): total += i\n• print(total)",
            hint: "Создай переменную total = 0. Затем цикл for i in range(1, 101): и внутри прибавляй i к total (total += i). После цикла выведи total.",
            type: "code",
            validator: (code) => code.includes('for') && code.includes('range')
        }
    },
    
    B1_11: {
        title: "Практика: while и break",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Найдём первое число больше 50 в списке, используя цикл while и break.</p>
        `,
        task: {
            description: "Найдите первое число больше 50 в списке [10, 20, 30, 60, 70] с помощью while и break.",
            requirements: "• numbers = [10, 20, 30, 60, 70]\n• i = 0\n• while i < len(numbers):\n    if numbers[i] > 50: break\n    i += 1",
            hint: "Создай переменную i = 0. В цикле while i < len(numbers) проверяй numbers[i] > 50. Если да — выведи число и break. Иначе увеличивай i на 1.",
            type: "code",
            validator: (code) => code.includes('while') && code.includes('break')
        }
    },
    
    B1_12: {
        title: "Практика: вложенные циклы",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Выведем координаты всех точек на сетке 3x3.</p>
        `,
        task: {
            description: "Напишите вложенный цикл для вывода координат (x, y) от 0 до 2.",
            requirements: "• for x in range(3):\n    for y in range(3):\n        print(x, y)",
            hint: "Используй внешний цикл for x in range(3): и внутри него — внутренний for y in range(3):. Внутри выводи x и y через print().",
            type: "code",
            validator: (code) => code.includes('for x') && code.includes('for y')
        }
    },
    
    B1_13: {
        title: "Практика: enumerate",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Используем enumerate для вывода элементов списка с индексами.</p>
        `,
        task: {
            description: "Вставьте пропущенное слово, чтобы вывести значение вместе с индексом.",
            type: "fill",
            code: "for i, val in enumerate([10,20,30]):\n    print(i, ___)",
            correct: "val"
        }
    },
    
    B1_14: {
        title: "Повторение модуля B1",
        theory: `
            <h4><i class="fas fa-review"></i> Повторение: что мы изучили в модуле B1</h4>
            <ul>
                <li>Цикл for и range()</li>
                <li>Цикл while</li>
                <li>Операторы break и continue</li>
                <li>Вложенные циклы</li>
                <li>Бесконечные циклы</li>
                <li>Функция enumerate()</li>
            </ul>
        `,
        task: {
            description: "Что делает break?",
            type: "checkbox",
            options: ["Прерывает цикл", "Пропускает итерацию", "Завершает программу"],
            correct: [0]
        }
    },

    // ==================== МОДУЛЬ B2 (Функции) ====================
    B2_1: {
        title: "Создание функций def",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое функция?</h4>
            <p>Функция — это блок кода, который можно вызывать многократно. Она помогает избежать повторения кода и делает программу более структурированной.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>def имя_функции():
    # код функции</code></pre>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>def say_hello():
    print("Hello!")

say_hello()  # вызов функции</code></pre>
        `,
        task: {
            description: "Создайте функцию say_hello, которая выводит 'Hello!'.",
            requirements: "• def say_hello():\n    print('Hello!')",
            hint: "Используй ключевое слово def, затем имя функции say_hello, затем круглые скобки и двоеточие. На следующей строке с отступом напиши print('Hello!').",
            type: "code",
            validator: (code) => code.includes('def') && code.includes('say_hello')
        }
    },
    
    B2_2: {
        title: "Аргументы функций",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое аргументы?</h4>
            <p>Аргументы — это данные, которые передаются в функцию для обработки. Они указываются в скобках при определении функции.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>def greet(name):
    print(f"Привет, {name}!")

greet("Анна")  # Привет, Анна!</code></pre>
        `,
        task: {
            description: "Создайте функцию greet(name), которая выводит 'Привет, [name]!'.",
            requirements: "• def greet(name):\n    print(f'Привет, {name}!')",
            hint: "Определи функцию с параметром name: def greet(name):. Внутри используй f-строку для вывода приветствия.",
            type: "code",
            validator: (code) => code.includes('def') && code.includes('name')
        }
    },
    
    B2_3: {
        title: "Оператор return",
        theory: `
            <h4><i class="fas fa-return"></i> Что такое return?</h4>
            <p>Оператор <code>return</code> возвращает значение из функции. Функция без return возвращает <code>None</code>.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>def add(a, b):
    return a + b

result = add(3, 5)
print(result)  # 8</code></pre>
        `,
        task: {
            description: "Создайте функцию add(a, b), которая возвращает сумму a и b.",
            requirements: "• def add(a, b):\n    return a + b",
            hint: "Определи функцию add с параметрами a и b. Внутри используй return a + b, чтобы вернуть сумму.",
            type: "code",
            validator: (code) => code.includes('def') && code.includes('return')
        }
    },
    
    B2_4: {
        title: "Значение None",
        theory: `
            <h4><i class="fas fa-ban"></i> Что такое None?</h4>
            <p><code>None</code> — это специальное значение в Python, означающее "ничего" или "отсутствие значения". Функции без <code>return</code> возвращают <code>None</code>.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>def say_hello():
    print("Hello")

result = say_hello()
print(result)  # None</code></pre>
        `,
        task: {
            description: "Что вернёт функция без return?",
            type: "explain",
            question: "Что вернёт функция без return?",
            options: ["None", "0", "Ошибку"],
            correct: 0
        }
    },
    
    B2_5: {
        title: "Область видимости",
        theory: `
            <h4><i class="fas fa-globe"></i> Что такое область видимости?</h4>
            <p>Область видимости определяет, где переменная доступна. Переменные, созданные внутри функции, видны только внутри неё (локальные). Переменные, созданные вне функций, видны везде (глобальные).</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>x = 10  # глобальная переменная

def test():
    x = 5  # локальная переменная
    print(x)  # 5

test()
print(x)  # 10</code></pre>
        `,
        task: {
            description: "Создайте функцию, которая изменяет глобальную переменную x с помощью global.",
            requirements: "• x = 10\n• def change():\n    global x\n    x = 20",
            hint: "Сначала создай глобальную переменную x = 10. Внутри функции change() используй ключевое слово global x, затем присвой x = 20.",
            type: "code",
            validator: (code) => code.includes('global')
        }
    },
    
    B2_6: {
        title: "*args — произвольное число аргументов",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое *args?</h4>
            <p><code>*args</code> позволяет передать в функцию произвольное количество позиционных аргументов. Все аргументы собираются в кортеж.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>def sum_all(*args):
    return sum(args)

print(sum_all(1, 2, 3))  # 6
print(sum_all(5, 10, 15, 20))  # 50</code></pre>
        `,
        task: {
            description: "Создайте функцию с *args, которая суммирует все переданные аргументы.",
            requirements: "• def sum_all(*args):\n    return sum(args)",
            hint: "Определи функцию sum_all с параметром *args. Внутри используй return sum(args).",
            type: "code",
            validator: (code) => code.includes('*args')
        }
    },
    
    B2_7: {
        title: "**kwargs — именованные аргументы",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое **kwargs?</h4>
            <p><code>**kwargs</code> позволяет передать произвольное количество именованных аргументов. Они собираются в словарь.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>def print_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_info(name="Анна", age=25, city="Москва")</code></pre>
        `,
        task: {
            description: "Создайте функцию с **kwargs, которая выводит все переданные именованные аргументы.",
            requirements: "• def print_info(**kwargs):\n    for key, value in kwargs.items():\n        print(key, value)",
            hint: "Определи функцию с параметром **kwargs. Внутри используй цикл for key, value in kwargs.items(): и выводи их.",
            type: "code",
            validator: (code) => code.includes('**kwargs')
        }
    },
    
    B2_8: {
        title: "lambda-функции",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое lambda?</h4>
            <p><code>lambda</code> — это анонимная функция, которая создаётся в одной строке. Часто используется для простых операций.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>lambda аргументы: выражение</code></pre>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>square = lambda x: x * x
print(square(5))  # 25</code></pre>
        `,
        task: {
            description: "Создайте lambda-функцию, которая удваивает число.",
            requirements: "• lambda x: x * 2",
            hint: "Напиши lambda x: x * 2. Это анонимная функция, которая принимает x и возвращает x * 2.",
            type: "code",
            validator: (code) => code.includes('lambda')
        }
    },
    
    B2_9: {
        title: "Функция map()",
        theory: `
            <h4><i class="fas fa-code"></i> Что делает map()?</h4>
            <p>Функция <code>map()</code> применяет заданную функцию к каждому элементу итерируемого объекта (списка, кортежа и т.д.) и возвращает итератор с результатами.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>numbers = [1, 2, 3, 4]
squared = list(map(lambda x: x * x, numbers))
print(squared)  # [1, 4, 9, 16]</code></pre>
        `,
        task: {
            description: "Используйте map и lambda для удвоения элементов списка [1, 2, 3].",
            requirements: "• list(map(lambda x: x * 2, [1,2,3]))",
            hint: "Используй map(lambda x: x * 2, [1,2,3]) и оберни результат в list(), чтобы получить список.",
            type: "code",
            validator: (code) => code.includes('map') && code.includes('lambda')
        }
    },
    
    B2_10: {
        title: "Функция filter()",
        theory: `
            <h4><i class="fas fa-code"></i> Что делает filter()?</h4>
            <p>Функция <code>filter()</code> фильтрует элементы итерируемого объекта, оставляя только те, для которых функция-предикат вернула True.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>numbers = [1, 2, 3, 4, 5, 6]
even = list(filter(lambda x: x % 2 == 0, numbers))
print(even)  # [2, 4, 6]</code></pre>
        `,
        task: {
            description: "Используйте filter и lambda для получения чётных чисел из списка [1,2,3,4,5,6].",
            requirements: "• list(filter(lambda x: x % 2 == 0, [1,2,3,4,5,6]))",
            hint: "Используй filter(lambda x: x % 2 == 0, [1,2,3,4,5,6]) и оберни в list(). Условие x % 2 == 0 проверяет чётность.",
            type: "code",
            validator: (code) => code.includes('filter') && code.includes('lambda')
        }
    },
    
    B2_11: {
        title: "Рекурсия",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое рекурсия?</h4>
            <p>Рекурсия — это когда функция вызывает саму себя. Она используется для решения задач, которые можно разбить на подзадачи того же типа (например, вычисление факториала, обход дерева).</p>
            
            <h4><i class="fas fa-code"></i> Пример (факториал):</h4>
            <pre><code>def factorial(n):
    if n <= 1:
        return 1
    return n * factorial(n - 1)

print(factorial(5))  # 120</code></pre>
        `,
        task: {
            description: "Напишите рекурсивную функцию factorial(n), которая вычисляет факториал числа n.",
            requirements: "• def factorial(n):\n    if n <= 1: return 1\n    return n * factorial(n-1)",
            hint: "Определи функцию factorial(n). Базовый случай: если n <= 1, верни 1. Иначе верни n * factorial(n-1).",
            type: "code",
            validator: (code) => code.includes('def') && code.includes('factorial') && code.includes('return')
        }
    },
    
    B2_12: {
        title: "Документация функций (docstring)",
        theory: `
            <h4><i class="fas fa-file-alt"></i> Что такое docstring?</h4>
            <p>Docstring (документирующая строка) — это строка документации, которая описывает, что делает функция. Она пишется сразу после определения функции в тройных кавычках.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>def add(a, b):
    """Возвращает сумму двух чисел."""
    return a + b

print(add.__doc__)  # Возвращает сумму двух чисел.</code></pre>
        `,
        task: {
            description: "Добавьте docstring к функции add(a, b) с описанием 'Возвращает сумму a и b'.",
            requirements: "• def add(a, b):\n    \"\"\"Возвращает сумму a и b\"\"\"\n    return a + b",
            hint: 'Сразу после строки def добавь строку в тройных кавычках с описанием функции.',
            type: "code",
            validator: (code) => code.includes('"""') || code.includes("'''")
        }
    },
    
    B2_13: {
        title: "Аргументы по умолчанию",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое аргументы по умолчанию?</h4>
            <p>Аргументы по умолчанию позволяют задать значение параметра, которое будет использоваться, если при вызове функции этот аргумент не передан.</p>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>def greet(name="Гость"):
    print(f"Привет, {name}!")

greet("Анна")  # Привет, Анна!
greet()        # Привет, Гость!</code></pre>
        `,
        task: {
            description: "Создайте функцию greet(name) с аргументом по умолчанию 'Гость'.",
            requirements: "• def greet(name='Гость'):\n    print(f'Привет, {name}!')",
            hint: "В определении функции укажи name='Гость'. Если аргумент не передан, будет использовано это значение.",
            type: "code",
            validator: (code) => code.includes('def') && code.includes('=')
        }
    },
    
    B2_14: {
        title: "Повторение модуля B2",
        theory: `
            <h4><i class="fas fa-review"></i> Повторение: что мы изучили в модуле B2</h4>
            <ul>
                <li>Создание функций def</li>
                <li>Аргументы и return</li>
                <li>None и область видимости</li>
                <li>*args и **kwargs</li>
                <li>lambda-функции</li>
                <li>map() и filter()</li>
                <li>Рекурсия</li>
                <li>Docstring и аргументы по умолчанию</li>
            </ul>
        `,
        task: {
            description: "Что делает return?",
            type: "checkbox",
            options: ["Возвращает значение", "Выводит значение", "Завершает функцию"],
            correct: [0, 2]
        }
    },

    // ==================== МОДУЛЬ C1 (Структуры данных) ====================
    C1_1: {
        title: "Списки",
        theory: `
            <h4><i class="fas fa-list"></i> Что такое список?</h4>
            <p>Список (list) — это упорядоченная коллекция элементов, которая может содержать данные разных типов. Списки изменяемы — элементы можно добавлять, удалять и изменять.</p>
            
            <h4><i class="fas fa-code"></i> Создание списка:</h4>
            <pre><code>numbers = [1, 2, 3, 4, 5]
mixed = [1, "hello", 3.14, True]
empty = []</code></pre>
            
            <h4><i class="fas fa-code"></i> Доступ к элементам:</h4>
            <pre><code>numbers = [10, 20, 30]
print(numbers[0])  # 10
print(numbers[1])  # 20
print(numbers[-1]) # 30 (последний элемент)</code></pre>
        `,
        task: {
            description: "Создайте список из 5 чисел: 1, 2, 3, 4, 5.",
            requirements: "• numbers = [1, 2, 3, 4, 5]",
            hint: "Список создаётся с помощью квадратных скобок. Перечисли числа через запятую: [1, 2, 3, 4, 5].",
            type: "code",
            validator: (code) => code.includes('[') && code.includes(']')
        }
    },
    
    C1_2: {
        title: "Методы списков",
        theory: `
            <h4><i class="fas fa-code"></i> Основные методы списков</h4>
            <ul>
                <li><code>append(x)</code> — добавляет элемент x в конец списка</li>
                <li><code>insert(i, x)</code> — вставляет элемент x на позицию i</li>
                <li><code>remove(x)</code> — удаляет первый элемент со значением x</li>
                <li><code>pop(i)</code> — удаляет элемент на позиции i и возвращает его</li>
                <li><code>sort()</code> — сортирует список</li>
                <li><code>reverse()</code> — переворачивает список</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>numbers = [1, 2, 3]
numbers.append(4)     # [1, 2, 3, 4]
numbers.insert(0, 0)  # [0, 1, 2, 3, 4]
numbers.pop()         # удаляет 4
numbers.sort()        # сортирует</code></pre>
        `,
        task: {
            description: "Создайте список [1,2,3] и добавьте в него элемент 4 с помощью append.",
            requirements: "• numbers = [1,2,3]\n• numbers.append(4)",
            hint: "Сначала создай список [1,2,3]. Затем используй метод .append(4) для добавления элемента в конец.",
            type: "code",
            validator: (code) => code.includes('append')
        }
    },
    
    C1_3: {
        title: "Срезы списков",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое срезы?</h4>
            <p>Срезы (slices) позволяют получить подсписок из списка. Синтаксис: <code>список[начало:конец:шаг]</code>.</p>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>numbers = [1, 2, 3, 4, 5]
print(numbers[1:4])   # [2, 3, 4]
print(numbers[:3])    # [1, 2, 3]
print(numbers[2:])    # [3, 4, 5]
print(numbers[::2])   # [1, 3, 5]</code></pre>
        `,
        task: {
            description: "Вставьте пропущенный индекс, чтобы получить элемент 3 из списка [1,2,3,4,5].",
            type: "fill",
            code: "nums = [1,2,3,4,5]\nprint(nums[___])  # выведет 3",
            correct: "2"
        }
    },
    
    C1_4: {
        title: "Словари",
        theory: `
            <h4><i class="fas fa-book"></i> Что такое словарь?</h4>
            <p>Словарь (dict) — это неупорядоченная коллекция пар "ключ-значение". Ключи уникальны и неизменяемы (обычно строки или числа), значения могут быть любыми.</p>
            
            <h4><i class="fas fa-code"></i> Создание словаря:</h4>
            <pre><code>user = {"name": "Анна", "age": 25, "city": "Москва"}
empty = {}</code></pre>
            
            <h4><i class="fas fa-code"></i> Доступ к значениям:</h4>
            <pre><code>print(user["name"])  # Анна
print(user["age"])   # 25</code></pre>
        `,
        task: {
            description: "Создайте словарь с ключами 'name' и 'age' и значениями 'Анна' и 25.",
            requirements: "• user = {'name': 'Анна', 'age': 25}",
            hint: "Словарь создаётся в фигурных скобках. Пары ключ:значение разделяются запятыми.",
            type: "code",
            validator: (code) => code.includes('{') && code.includes('}') && code.includes(':')
        }
    },
    
    C1_5: {
        title: "Методы словарей",
        theory: `
            <h4><i class="fas fa-code"></i> Основные методы словарей</h4>
            <ul>
                <li><code>keys()</code> — возвращает все ключи</li>
                <li><code>values()</code> — возвращает все значения</li>
                <li><code>items()</code> — возвращает пары (ключ, значение)</li>
                <li><code>get(key)</code> — возвращает значение по ключу (без ошибки, если ключа нет)</li>
                <li><code>pop(key)</code> — удаляет элемент по ключу</li>
            </ul>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>user = {"name": "Анна", "age": 25}
print(user.keys())   # dict_keys(['name', 'age'])
print(user.values()) # dict_values(['Анна', 25])
print(user.items())  # dict_items([('name', 'Анна'), ('age', 25)])</code></pre>
        `,
        task: {
            description: "Получите значение по ключу 'name' из словаря {'name': 'Анна', 'age': 25}.",
            requirements: "• user['name']",
            hint: "Используй квадратные скобки и ключ: user['name'].",
            type: "code",
            validator: (code) => code.includes('[') && code.includes(']') && code.includes("'name'")
        }
    },
    
    C1_6: {
        title: "Множества",
        theory: `
            <h4><i class="fas fa-cube"></i> Что такое множество?</h4>
            <p>Множество (set) — это неупорядоченная коллекция уникальных элементов. В множестве не может быть дубликатов.</p>
            
            <h4><i class="fas fa-code"></i> Создание множества:</h4>
            <pre><code>numbers = {1, 2, 3, 4, 5}
letters = set("hello")  # {'h', 'e', 'l', 'o'}
empty = set()</code></pre>
            
            <h4><i class="fas fa-code"></i> Операции с множествами:</h4>
            <pre><code>a = {1, 2, 3}
b = {2, 3, 4}
print(a | b)  # объединение: {1,2,3,4}
print(a & b)  # пересечение: {2,3}
print(a - b)  # разность: {1}</code></pre>
        `,
        task: {
            description: "Создайте множество из чисел 1, 2, 3.",
            requirements: "• numbers = {1, 2, 3}",
            hint: "Множество создаётся в фигурных скобках без двоеточий (в отличие от словаря): {1, 2, 3}.",
            type: "code",
            validator: (code) => code.includes('{') && code.includes('}') && !code.includes(':')
        }
    },
    
    C1_7: {
        title: "Кортежи",
        theory: `
            <h4><i class="fas fa-database"></i> Что такое кортеж?</h4>
            <p>Кортеж (tuple) — это неизменяемая упорядоченная коллекция. После создания кортежа нельзя изменить его элементы.</p>
            
            <h4><i class="fas fa-code"></i> Создание кортежа:</h4>
            <pre><code>numbers = (1, 2, 3)
mixed = (1, "hello", 3.14)
single = (5,)  # кортеж из одного элемента (запятая важна!)</code></pre>
            
            <h4><i class="fas fa-code"></i> Доступ к элементам:</h4>
            <pre><code>print(numbers[0])  # 1
print(numbers[1])  # 2</code></pre>
        `,
        task: {
            description: "Создайте кортеж из трёх элементов: 1, 2, 3.",
            requirements: "• t = (1, 2, 3)",
            hint: "Кортеж создаётся в круглых скобках. Перечисли элементы через запятую: (1, 2, 3).",
            type: "code",
            validator: (code) => code.includes('(') && code.includes(')')
        }
    },
    
    C1_8: {
        title: "List comprehension",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое list comprehension?</h4>
            <p>List comprehension (генератор списка) — это компактный способ создания списков на основе существующих последовательностей.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>[выражение for элемент in последовательность if условие]</code></pre>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>squares = [x * x for x in range(5)]  # [0, 1, 4, 9, 16]
evens = [x for x in range(10) if x % 2 == 0]  # [0, 2, 4, 6, 8]</code></pre>
        `,
        task: {
            description: "Создайте список квадратов чисел от 1 до 5 с помощью list comprehension.",
            requirements: "• [x*x for x in range(1, 6)]",
            hint: "Используй синтаксис [x*x for x in range(1, 6)]. range(1, 6) даст числа от 1 до 5.",
            type: "code",
            validator: (code) => code.includes('for') && code.includes(']')
        }
    },
    
    C1_9: {
        title: "Dict comprehension",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое dict comprehension?</h4>
            <p>Dict comprehension — это компактный способ создания словарей на основе существующих последовательностей.</p>
            
            <h4><i class="fas fa-code"></i> Синтаксис:</h4>
            <pre><code>{ключ: значение for элемент in последовательность if условие}</code></pre>
            
            <h4><i class="fas fa-code"></i> Пример:</h4>
            <pre><code>squares = {x: x*x for x in range(1, 6)}  # {1:1, 2:4, 3:9, 4:16, 5:25}</code></pre>
        `,
        task: {
            description: "Создайте словарь квадратов чисел от 1 до 5 с помощью dict comprehension.",
            requirements: "• {x: x*x for x in range(1, 6)}",
            hint: "Используй синтаксис {x: x*x for x in range(1, 6)}. Ключом будет x, значением — x*x.",
            type: "code",
            validator: (code) => code.includes('for') && code.includes(':')
        }
    },
    
    C1_10: {
        title: "Распаковка",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое распаковка?</h4>
            <p>Распаковка (unpacking) — это присваивание элементов последовательности отдельным переменным.</p>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>a, b = [1, 2]   # a = 1, b = 2
x, y, z = (10, 20, 30)  # x = 10, y = 20, z = 30
first, *rest = [1, 2, 3, 4]  # first = 1, rest = [2, 3, 4]</code></pre>
        `,
        task: {
            description: "Распакуйте список [1, 2] в переменные a и b.",
            requirements: "• a, b = [1, 2]",
            hint: "Напиши a, b = [1, 2]. Слева от равно — переменные через запятую, справа — список.",
            type: "code",
            validator: (code) => code.includes('=') && code.includes(',')
        }
    },
    
    C1_11: {
        title: "Копирование списков",
        theory: `
            <h4><i class="fas fa-copy"></i> Как копировать списки?</h4>
            <p>Простое присваивание (a = b) не создаёт копию — обе переменные ссылаются на один и тот же объект. Для создания копии нужно использовать <code>copy()</code> или срез <code>[:]</code>.</p>
            
            <h4><i class="fas fa-code"></i> Примеры:</h4>
            <pre><code>original = [1, 2, 3]
shallow_copy = original.copy()  # копия
shallow_copy2 = original[:]      # тоже копия
deep_copy = copy.deepcopy(original)  # для вложенных списков</code></pre>
        `,
        task: {
            description: "Скопируйте список [1,2,3] с помощью метода copy().",
            requirements: "• original = [1,2,3]\n• copy = original.copy()",
            hint: "Сначала создай список original. Затем создай новую переменную и вызови метод .copy() у original.",
            type: "code",
            validator: (code) => code.includes('copy()')
        }
    },
    
    C1_12: {
        title: "Практика: списки",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Найдём сумму всех элементов списка. Это полезно для анализа данных.</p>
        `,
        task: {
            description: "Найдите сумму всех элементов списка [10, 20, 30, 40, 50] с помощью функции sum().",
            requirements: "• numbers = [10,20,30,40,50]\n• print(sum(numbers))",
            hint: "Функция sum() принимает список и возвращает сумму всех элементов.",
            type: "code",
            validator: (code) => code.includes('sum')
        }
    },
    
    C1_13: {
        title: "Практика: словари",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Отсортируем список чисел.</p>
        `,
        task: {
            description: "Отсортируйте список [3, 1, 4, 1, 5, 9] с помощью метода sort() или функции sorted().",
            requirements: "• numbers = [3,1,4,1,5,9]\n• numbers.sort() или sorted(numbers)",
            hint: "Метод .sort() сортирует список на месте. Функция sorted() возвращает новый отсортированный список.",
            type: "code",
            validator: (code) => code.includes('sort') || code.includes('sorted')
        }
    },
    
    C1_14: {
        title: "Повторение модуля C1",
        theory: `
            <h4><i class="fas fa-review"></i> Повторение: что мы изучили в модуле C1</h4>
            <ul>
                <li>Списки и их методы</li>
                <li>Срезы списков</li>
                <li>Словари и их методы</li>
                <li>Множества</li>
                <li>Кортежи</li>
                <li>List и dict comprehension</li>
                <li>Распаковка и копирование</li>
            </ul>
        `,
        task: {
            description: "Как создать список?",
            type: "checkbox",
            options: ["[]", "{}", "()"],
            correct: [0]
        }
    },

    // ==================== МОДУЛЬ C2 (Продвинутые темы) ====================
    C2_1: {
        title: "Функция enumerate()",
        theory: `
            <h4><i class="fas fa-list-ol"></i> Что делает enumerate()?</h4>
            <p>enumerate() возвращает индекс и значение элемента при итерации.</p>
            <pre><code>fruits = ['яблоко', 'банан', 'апельсин']
for i, fruit in enumerate(fruits):
    print(i, fruit)</code></pre>
        `,
        task: {
            description: "Используйте enumerate для вывода индекса и значения списка ['a','b','c'].",
            type: "code",
            validator: (code) => code.includes('enumerate')
        }
    },
    
    C2_2: {
        title: "Функция zip()",
        theory: `
            <h4><i class="fas fa-code"></i> Что делает zip()?</h4>
            <p>zip() объединяет несколько последовательностей в кортежи.</p>
            <pre><code>names = ['Анна', 'Петр']
ages = [25, 30]
for name, age in zip(names, ages):
    print(name, age)</code></pre>
        `,
        task: {
            description: "Объедините два списка ['a','b'] и [1,2] с помощью zip.",
            type: "code",
            validator: (code) => code.includes('zip')
        }
    },
    
    C2_3: {
        title: "Функция sorted()",
        theory: `
            <h4><i class="fas fa-sort"></i> Что делает sorted()?</h4>
            <p>sorted() возвращает отсортированный список из любой итерируемой последовательности.</p>
            <pre><code>numbers = [3, 1, 4, 1, 5]
sorted_numbers = sorted(numbers)  # [1, 1, 3, 4, 5]</code></pre>
        `,
        task: {
            description: "Отсортируйте список [3,1,4,1,5] с помощью sorted.",
            type: "code",
            validator: (code) => code.includes('sorted')
        }
    },
    
    C2_4: {
        title: "Функция any()",
        theory: `
            <h4><i class="fas fa-code"></i> Что делает any()?</h4>
            <p>any() возвращает True, если хотя бы один элемент в последовательности истинен.</p>
            <pre><code>print(any([False, False, True]))  # True
print(any([False, False, False])) # False</code></pre>
        `,
        task: {
            description: "Проверьте, есть ли True в списке [False, False, True] с помощью any.",
            type: "code",
            validator: (code) => code.includes('any')
        }
    },
    
    C2_5: {
        title: "Функция all()",
        theory: `
            <h4><i class="fas fa-code"></i> Что делает all()?</h4>
            <p>all() возвращает True, если все элементы последовательности истинны.</p>
            <pre><code>print(all([True, True, True]))   # True
print(all([True, False, True]))  # False</code></pre>
        `,
        task: {
            description: "Проверьте, все ли элементы True в списке [True, True, True] с помощью all.",
            type: "code",
            validator: (code) => code.includes('all')
        }
    },
    
    C2_6: {
        title: "Обработка ошибок try/except",
        theory: `
            <h4><i class="fas fa-exclamation-triangle"></i> Что такое try/except?</h4>
            <p>try/except позволяет обрабатывать ошибки (исключения) без остановки программы.</p>
            <pre><code>try:
    x = int(input())
except ValueError:
    print("Ошибка: введите число!")</code></pre>
        `,
        task: {
            description: "Напишите try/except для обработки ошибки деления на ноль.",
            type: "code",
            validator: (code) => code.includes('try') && code.includes('except')
        }
    },
    
    C2_7: {
        title: "Работа с файлами: open()",
        theory: `
            <h4><i class="fas fa-file"></i> Как открыть файл?</h4>
            <p>open() открывает файл. Режимы: 'r' (чтение), 'w' (запись), 'a' (добавление).</p>
            <pre><code>file = open("test.txt", "r")
content = file.read()
file.close()</code></pre>
        `,
        task: {
            description: "Откройте файл 'data.txt' для чтения (режим 'r').",
            type: "code",
            validator: (code) => code.includes('open') && code.includes('"r"')
        }
    },
    
    C2_8: {
        title: "Чтение файлов: read()",
        theory: `
            <h4><i class="fas fa-file-alt"></i> Как прочитать файл?</h4>
            <p>read() читает всё содержимое файла, readline() — одну строку, readlines() — список строк.</p>
            <pre><code>content = file.read()
line = file.readline()
lines = file.readlines()</code></pre>
        `,
        task: {
            description: "Прочитайте содержимое файла 'data.txt' с помощью read().",
            type: "code",
            validator: (code) => code.includes('.read()')
        }
    },
    
    C2_9: {
        title: "Запись в файлы: write()",
        theory: `
            <h4><i class="fas fa-file-export"></i> Как записать в файл?</h4>
            <p>write() записывает строку в файл. Файл должен быть открыт в режиме 'w' или 'a'.</p>
            <pre><code>file = open("test.txt", "w")
file.write("Hello, world!")
file.close()</code></pre>
        `,
        task: {
            description: "Запишите строку 'Hello, world!' в файл 'output.txt'.",
            type: "code",
            validator: (code) => code.includes('.write') && code.includes('"w"')
        }
    },
    
    C2_10: {
        title: "Модули и import",
        theory: `
            <h4><i class="fas fa-cube"></i> Что такое модули?</h4>
            <p>Модули — это файлы с кодом, которые можно импортировать. import подключает модуль.</p>
            <pre><code>import math
print(math.sqrt(16))  # 4.0

from random import randint
print(randint(1, 10))</code></pre>
        `,
        task: {
            description: "Импортируйте модуль math.",
            type: "code",
            validator: (code) => code.includes('import math')
        }
    },
    
    C2_11: {
        title: "Модуль datetime",
        theory: `
            <h4><i class="fas fa-calendar"></i> Что такое datetime?</h4>
            <p>datetime — модуль для работы с датой и временем.</p>
            <pre><code>from datetime import datetime
now = datetime.now()
print(now.year, now.month, now.day)</code></pre>
        `,
        task: {
            description: "Выведите текущую дату с помощью datetime.now().",
            type: "code",
            validator: (code) => code.includes('datetime')
        }
    },
    
    C2_12: {
        title: "Модуль random",
        theory: `
            <h4><i class="fas fa-dice"></i> Что делает random?</h4>
            <p>random — модуль для генерации случайных чисел.</p>
            <pre><code>from random import randint, choice
print(randint(1, 10))      # случайное число от 1 до 10
print(choice(['a', 'b', 'c']))  # случайный элемент</code></pre>
        `,
        task: {
            description: "Сгенерируйте случайное число от 1 до 10 с помощью randint.",
            type: "code",
            validator: (code) => code.includes('random') && code.includes('randint')
        }
    },
    
    C2_13: {
        title: "Практика: try-except-else",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Используем try-except-else для обработки ошибок. Блок else выполняется, если не было исключения.</p>
        `,
        task: {
            description: "Напишите try-except-else для преобразования строки в число.",
            type: "code",
            validator: (code) => code.includes('else') && code.includes('except')
        }
    },
    
    C2_14: {
        title: "Повторение модуля C2",
        theory: `
            <h4><i class="fas fa-review"></i> Повторение модуля C2</h4>
            <ul>
                <li>enumerate, zip, sorted</li>
                <li>any и all</li>
                <li>try/except</li>
                <li>Работа с файлами</li>
                <li>Модули datetime, random</li>
            </ul>
        `,
        task: {
            description: "Что делает try/except?",
            type: "checkbox",
            options: ["Обрабатывает ошибки", "Создаёт цикл", "Определяет функцию"],
            correct: [0]
        }
    },

    // ==================== МОДУЛЬ C3 (ООП и финал) ====================
    C3_1: {
        title: "Классы и объекты",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое класс?</h4>
            <p>Класс — это шаблон для создания объектов. Объект — экземпляр класса.</p>
            <pre><code>class Car:
    pass

my_car = Car()</code></pre>
        `,
        task: {
            description: "Создайте класс Car.",
            type: "code",
            validator: (code) => code.includes('class')
        }
    },
    
    C3_2: {
        title: "Метод __init__",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое __init__?</h4>
            <p>__init__ — конструктор, вызывается при создании объекта.</p>
            <pre><code>class Car:
    def __init__(self, brand):
        self.brand = brand</code></pre>
        `,
        task: {
            description: "Добавьте метод __init__ в класс Car с параметром brand.",
            type: "code",
            validator: (code) => code.includes('__init__')
        }
    },
    
    C3_3: {
        title: "Параметр self",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое self?</h4>
            <p>self — ссылка на текущий экземпляр класса. Используется для доступа к атрибутам и методам объекта.</p>
            <pre><code>class Car:
    def __init__(self, brand):
        self.brand = brand
    
    def info(self):
        print(self.brand)</code></pre>
        `,
        task: {
            description: "Создайте метод info в классе Car с self.",
            type: "code",
            validator: (code) => code.includes('self') && code.includes('def')
        }
    },
    
    C3_4: {
        title: "Наследование",
        theory: `
            <h4><i class="fas fa-code-branch"></i> Что такое наследование?</h4>
            <p>Наследование позволяет создать новый класс на основе существующего.</p>
            <pre><code>class Vehicle:
    pass

class Car(Vehicle):
    pass</code></pre>
        `,
        task: {
            description: "Создайте класс ElectricCar, наследующий от Car.",
            type: "code",
            validator: (code) => code.includes('class') && code.includes('(')
        }
    },
    
    C3_5: {
        title: "Магические методы",
        theory: `
            <h4><i class="fas fa-magic"></i> Что такое магические методы?</h4>
            <p>Магические методы (dunder methods) — это методы с двойным подчёркиванием: __str__, __repr__, __len__ и др.</p>
            <pre><code>class Car:
    def __str__(self):
        return "Автомобиль"</code></pre>
        `,
        task: {
            description: "Добавьте метод __str__ в класс Car.",
            type: "code",
            validator: (code) => code.includes('__str__')
        }
    },
    
    C3_6: {
        title: "Декоратор property",
        theory: `
            <h4><i class="fas fa-code"></i> Что делает @property?</h4>
            <p>@property позволяет управлять доступом к атрибутам.</p>
            <pre><code>class Car:
    @property
    def brand(self):
        return self._brand</code></pre>
        `,
        task: {
            description: "Добавьте property в класс Car.",
            type: "code",
            validator: (code) => code.includes('@property')
        }
    },
    
    C3_7: {
        title: "Статические методы",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое @staticmethod?</h4>
            <p>Статические методы не требуют self и могут быть вызваны без создания объекта.</p>
            <pre><code>class Math:
    @staticmethod
    def add(a, b):
        return a + b</code></pre>
        `,
        task: {
            description: "Добавьте статический метод @staticmethod в класс Math.",
            type: "code",
            validator: (code) => code.includes('@staticmethod')
        }
    },
    
    C3_8: {
        title: "Исключения в ООП",
        theory: `
            <h4><i class="fas fa-exclamation-triangle"></i> Как вызвать исключение?</h4>
            <p>raise вызывает исключение.</p>
            <pre><code>raise ValueError("Ошибка!")</code></pre>
        `,
        task: {
            description: "Вызовите исключение ValueError с помощью raise.",
            type: "code",
            validator: (code) => code.includes('raise')
        }
    },
    
    C3_9: {
        title: "Практика: классы",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Создадим класс BankAccount с балансом.</p>
        `,
        task: {
            description: "Создайте класс BankAccount с атрибутом balance.",
            type: "code",
            validator: (code) => code.includes('class') && code.includes('balance')
        }
    },
    
    C3_10: {
        title: "Практика: создание объектов",
        theory: `
            <h4><i class="fas fa-practice"></i> Практическое задание</h4>
            <p>Создадим объект класса.</p>
        `,
        task: {
            description: "Создайте объект класса Car с именем my_car.",
            type: "code",
            validator: (code) => code.includes('=') && code.includes('(')
        }
    },
    
    C3_11: {
        title: "Переопределение методов",
        theory: `
            <h4><i class="fas fa-code"></i> Что такое переопределение?</h4>
            <p>Переопределение — это изменение метода родительского класса в дочернем.</p>
            <pre><code>class Car:
    def sound(self):
        return "Beep"

class ElectricCar(Car):
    def sound(self):
        return "Silence"</code></pre>
        `,
        task: {
            description: "Переопределите метод sound в классе ElectricCar.",
            type: "code",
            validator: (code) => code.includes('super()')
        }
    },
    
    C3_12: {
        title: "Магический метод __len__",
        theory: `
            <h4><i class="fas fa-code"></i> Что делает __len__?</h4>
            <p>__len__ возвращает длину объекта. Вызывается функцией len().</p>
            <pre><code>class MyList:
    def __len__(self):
        return 10</code></pre>
        `,
        task: {
            description: "Добавьте метод __len__ в класс MyList.",
            type: "code",
            validator: (code) => code.includes('__len__')
        }
    },
    
    C3_13: {
        title: "Метод класса @classmethod",
        theory: `
            <h4><i class="fas fa-code"></i> Что делает @classmethod?</h4>
            <p>Метод класса получает cls вместо self и может изменять состояние класса.</p>
            <pre><code>class Car:
    count = 0
    
    @classmethod
    def add_car(cls):
        cls.count += 1</code></pre>
        `,
        task: {
            description: "Создайте класс с @classmethod.",
            type: "code",
            validator: (code) => code.includes('@classmethod')
        }
    },
    
    C3_14: {
        title: "Повторение всего курса",
        theory: `
            <h4><i class="fas fa-review"></i> Поздравляем!</h4>
            <p>Вы прошли весь курс PyPath! Вы изучили:</p>
            <ul>
                <li>Основы Python (переменные, типы, ввод/вывод)</li>
                <li>Условия и логические операторы</li>
                <li>Циклы (for, while)</li>
                <li>Функции и рекурсию</li>
                <li>Структуры данных (списки, словари, множества, кортежи)</li>
                <li>Продвинутые темы (обработка ошибок, файлы, модули)</li>
                <li>Объектно-ориентированное программирование</li>
            </ul>
        `,
        task: {
            description: "Что такое класс?",
            type: "checkbox",
            options: ["Шаблон для объектов", "Функция", "Переменная"],
            correct: [0]
        }
    }
};

// Функция для получения контента урока
function getLessonContent(lessonId) {
    if (lessonsContent[lessonId]) {
        return lessonsContent[lessonId];
    }
    return {
        title: "Урок",
        theory: "<p>Теория загружается...</p>",
        task: { description: "Задание в разработке", requirements: "", hint: "", type: "code", validator: () => true }
    };
}